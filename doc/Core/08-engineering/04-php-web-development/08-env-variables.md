# 08. 环境变量与配置管理

概要：将环境相关配置从代码中分离，支持多环境部署与安全密钥管理。

最佳实践要点
- 使用环境变量加载配置，避免把密钥写死在代码中。
- 区分开发、测试、生产环境的配置，并有一致的命名规范。
- 对敏感信息采用合规的密钥管理方案。
- 使用配置缓存，减少对磁盘/网络的频繁读取。

落地 Demo
```php
// 假设使用 Dotenv 加载环境变量
$dbHost = getenv('DB_HOST');
```
