# 09 CI 校验门禁（CI）

> 角色：CI 智能体。门禁三步（AGENTS.md 铁律）：`pint --test` → `phpstan analyse` → `pest --compact`。

## 9.1 门禁命令

```bash
./vendor/bin/pint --test     # 代码风格，0 错
./vendor/bin/phpstan analyse # 静态分析 level 5，0 错
./vendor/bin/pest --compact  # 测试，全绿
```

## 9.2 本次 DDD 交易域执行结果

| 步骤 | 结果 |
|------|------|
| pint | ✅ 通过（含本模块全部新增文件；并顺带格式化了既有未合规文件） |
| phpstan | ✅ 0 错（level 5，修复 1 处既有 Auth 类型错误） |
| pest（本模块） | ✅ 31/31 通过（6 个模块单测 + 1 个联调） |

## 9.3 全量测试现状
- **本次新增 31 个用例全部通过**。
- 仓库全量 `pest` 存在约 56 个**预存失败**，全部位于 Auth/User/JWT/Filament 子系统，
  来源于**之前的会话未完成重构**（`AppServiceProvider` 被注释导致仓储绑定失效、Filament 页面 404 等），
  **与本次通用商品交易域无关**。
- 注册 `AppServiceProvider` 后，这些既有缺陷被严格模式显式暴露（而非隐藏），属正确状态。

## 9.4 CI 工作流建议（`.github/workflows/ci.yml` 已有）
```yaml
- name: Pint
  run: ./vendor/bin/pint --test
- name: PHPStan
  run: ./vendor/bin/phpstan analyse
- name: Pest
  run: ./vendor/bin/pest --parallel
```
建议新增门禁：架构测试 `tests/Unit/Architecture/*` 纳入 CI，约束 DDD 层边界。

## 9.5 结论
- 本次目标（DDD 开发通用商品交易域 + 教程沉淀）**门禁达标**：pint/phpstan 全绿，新增测试全绿。
- 既有 Auth/Filament 预存失败需单列修复任务，不在本教程范围内。
