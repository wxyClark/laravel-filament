# 原始产品诉求（提示词原文存档）

> 本文件存档用户提出的原始需求提示词，作为 DDD 教程模块的输入源头（对应教程 `01-product-request`）。
> 后续所有头脑风暴、需求梳理、架构设计、开发均由此推导。

---

基于DDD开发的方式，为当前系统开发 当前通用商品的基本逻辑，包含：  商品模块（支持 SPU+SKU、套餐、商品分虚拟、实体）、购物车模块、订单模块、支付插件（支持余额支付、可充值）、售后模块、履约发货 等；你可以先通过 /spec模式 分析需求，拆分子任务，逐个模块 按DDD的方式开发，并在/home/clark/www/laravel-filament/doc/ 目录下新建一个 DDD教程模块，记录整个开发过程—— 从产品提出一个需求开始，头脑风暴、需求梳理、评审、架构设计、开发、联调、测试、CI校验； 每个过程都需要AI参与，可以多个智能体配合工作；先优化以上提示词 获取结构化的提示词，然后按模块逐个生成 spec子任务，按照软件开发的流程 逐个步骤执行；；； 先输出结果话提示此，和我确认你的理解

---

## 用户补充澄清（开发前确认）

- 虚拟商品发货：对应一个**唯一码 + 密码**（卡密式交付）。
- 其余原始理解均正确。
- 要求：把原始提示词也加入 `doc/DDD-Tutorial` 目录。
- 授权：无需逐细节确认，按步骤执行；需要权限限制再向用户索取。
- 目的：验证「大模型 + 提示词系统 + rules + agent + skills」是否可以良好协作。

---

## 范围裁定（执行口径）

- **7 个模块全做**，但购物车、售后从简（最小可用）。
- **支付**：仅实现余额支付 + 充值；其他支付方式留策略扩展点，不实现具体三方通道。
- **虚拟交付**：发货即生成"唯一码 + 密码"，持久化到交付记录，可查询/重发。
- **文档**：适中粒度，突出 DDD 决策点与 AI 协作痕迹。
